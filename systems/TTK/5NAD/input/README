SEEKR input for 5NAD (BAY 1217389) System.
==========================================

This directory contains all files needed to prepare and run a HIDR/SEEKR
calculation for this system.

To prepare the SEEKR calculation from the inputs, run:

  python ~/seekr2/seekr2/prepare.py input_5NAD.xml

To run HIDR to generate the starting structures using MetaD:

  python ~/seekrtools/seekrtools/hidr/hidr.py any model.xml -M metadyn -p ref.pdb -c 0 -H 1.0

-- OR -- 

To run HIDR to generate the starting structures using SMD:

  python ~/seekrtools/seekrtools/hidr/hidr.py any model.xml -M SMD -p ref.pdb -c 0 -k 50000 -v 0.0165
